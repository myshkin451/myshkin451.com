# 随记：使用与验收

日期：2026-09-21。范围见 [决策 0014](../decisions/0014-single-author-notes.md)。

## 如何使用

站主登录后打开 `/studio/notes`，直接输入正文并发布，无需标题、摘要或封面。
「存草稿」仅保存到站主工作台；对已有随记存草稿时，访客继续阅读上次发布的内容。
「发布修改」更新公开副本；「撤下」隐藏公开内容并保留草稿，可再次发布。

「主题与留言」可选择主题和是否开放留言。新随记默认不开启留言，开启后使用原有登录、限流
和审核流程。支持普通文字、换行与 HTTP(S) 链接，不执行 HTML，不自动抓取链接预览。
每条最多 5,000 个字符，长文字在列表里可以展开；日期链接进入独立页面，可复制分享。

`/notes` 按初次发布时间倒序排列，可筛选月份、主题和文字。月份使用北京时间，编辑不会把
旧记录移到最前面。手机同样可编辑、发布和撤下。草稿保存需主动点击，离开有未保存修改的
编辑器会提示；不能保证浏览器崩溃时恢复尚未保存的文字。

## 本次验证

- 增量迁移应用于保留的本机 Supabase 栈，数据库 lint 无错误。
- 68 项前端测试通过，覆盖无标题发布、旧文章标题要求、草稿隔离、Unicode 字数边界、
  月份/主题筛选、链接安全、长文展开、保存失败保留文字、重复提交和草稿保存；登录返回、未发布状态、撤下后的链接和重复编辑链接保护。
- 186 项真实 API 断言通过，包含既有发布/媒体/身份/留言回归与新增随记的匿名及双访客
  越权检查、空白/长度校验、草稿隔离、修改、撤下、再发布和时间稳定性。
- 正式应用和独立前端构建通过，格式、lint、根目录和 site 类型检查通过。
- 两个独立浏览器来源验证真实本机数据库：无标题草稿 → 公开发布 → 私下修改/存草稿
  （匿名仍读原文）→ 发布修改 → 撤下（匿名不可读、草稿保留）。重启应用后草稿仍在。
- 1280px 桌面及 390/320px 手机检查随记时间线，320px 检查编辑器；页面无横向溢出。
  编辑器首次发现的标题校验和登录返回问题已修复并加入回归测试。
- 云端迁移 `202609210002` 已应用并登记历史，前置校验核对旧函数摘要和唯一旧迁移。
  不创建云端测试账号或样例；实际部署版本在合入主线后补记。

样例和测试账号只用于本机。云端仍须由本人完成真实账号验证与首次发布。

## 域名与邮件接续

本人明确选择推迟购买域名，不能把此前同意 `myshkin451.com` 当作仍有效的购买授权。
本次实际在 Porkbun 搜索，`myshkin451.com` 可加入购物车，首年及续费均显示 $11.08/年；
`myshkin451.blog` 首年 $2.57、续费 $21.11。查询没有保留域名，未来购买前重新核价。
官方[价格表](https://porkbun.com/products/domains)按非溢价域名列示，最终以本人结算页为准。

[Resend 免费档](https://resend.com/pricing)本次核实为 $0/月、3,000 封/月、100 封/日。
[Supabase SMTP 接入](https://resend.com/docs/send-with-supabase-smtp)需要验证自有域名；
配置为 `smtp.resend.com:465`、用户名 `resend`，发送凭据仅存 Supabase SMTP 设置。
未注册新服务、购买域名或启用付费套餐，未继续 Brevo 手机验证。

域名与真实邮件恢复接续后：本人购买/验证 → DNS/HTTPS/正式 origin 与回调 → SMTP 与实际
确认/恢复送达 → 首位站主授权 → 本人真实随记/长文/图片发布 → 重新部署后持久化及长期私密备份。
