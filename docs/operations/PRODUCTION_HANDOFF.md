# 生产上线与真实账号验收交接

更新：2026-09-27。以下“下一阶段接续”是当前执行依据；后半部分保留原始委托作为历史验收范围。
当前状态看 [progress.md](../../progress.md)，操作看 [运行说明](RUNBOOK.md)，认证准备看
[邮件接续](EMAIL_SETUP.md)。历史中的“尚无后端”和重新选择架构不再适用。

## 下一阶段接续

**用户已明确“这个版本就可以了”。当前现代视觉是上线基线，不再继续视觉方向探索。**
共享前端已接回真实内容和发布体系；`site/` + Supabase 后端、稳定路由、草稿隔离、媒体、
访客留言和站主权限均已实现，按[决策 0013](../decisions/0013-production-supabase-and-next.md)
与[决策 0014](../decisions/0014-single-author-notes.md)继续使用。不要重建后端或再交独立样稿。

### 已知起点

- 正式地址仍为 [myshkin451.vercel.app](https://myshkin451.vercel.app)。2026-09-27 从本机实时
  HTTP 核对：首页、`/login`、`/register`、`/recover` 返回 200，`/health` 返回
  `200 {"status":"ok"}`。公开 SSR 配置为 `emailEnabled=false`、`githubEnabled=false`；
  新版 `modern-home` 尚未上线。HTTP 成功不代表账号或真实发布已经验收。
- 本轮核对时远程 `main` 为 `9b916f8`，本地 `main` 为 `e264d44` 加已验收设计改动。
  本轮收尾将把改动保存在**本地 `main` 提交**，暂不 push；新任务先核实最终提交与差异，
  从本地交付提交接续，不能只拉取较旧远程分支覆盖成果。向远程 `main` 同步会触发既有
  Vercel 自动部署，由上线任务执行并验收。
- 完整本机预览是 `http://127.0.0.1:4333/index.html?preview=sample`，服务器保留给本人查看；
  自动化浏览器已关闭。`study.html` 仅作探索参照。IndexedDB 内容、样例与本机演示身份均
  不自动迁入 Supabase。接手时检查服务状态，不以预览中可进入工作台证明生产已有管理员。
- 站主与访客共用 Supabase 邮箱密码身份；站主需对已核实的 Auth UUID 显式授予
  `site_owners` 权限。没有默认生产管理员、固定初始密码或“首个注册自动成为站主”。
  当前邮件开关同时控制邮箱登录、注册和找回表单；真实首位站主尚未完成激活验收。
- 中文确认／恢复模板与本机 Auth 邮件流程已有验证；Hosted SMTP、真实外部收信、
  云端本人发布和长期私密备份仍待完成。本机邮箱捕获器及本机恢复证据不能替代这些验收。

### 执行顺序与完成标准

1. **固定并发布已验收新版。** 核实本地交付提交、远程差异与既有部署配置，保留 Cursor 和
   本轮成果；执行相应检查后同步已验收代码，部署到现有 Vercel 项目。验证实际线上首页、
   公开内容、账号／工作台边界、SSR、移动端及核心导航，记录生产提交和部署证据。
   不重复初始化 Supabase，不把样例、测试账号或本机数据库当作生产种子。
2. **激活真实认证邮件。** 当期核实可用邮件服务和域验证要求，确定具体方案后配置 SMTP、
   中文模板、精确回跳与限流，读回确认。Brevo 反复手机验证路径已停止，不重新尝试。
  Resend 是先前候选，尚无生产激活或验收记录，不把旧建议当成当前已选服务。
   网站继续使用 Vercel 地址，网站域名购买仍暂缓；如发件服务需要本人控制的域名，单独提出
   发件域的用途、可行选择与当期费用，不强迫先定网站品牌或购买网站域名。新花费仍须本人决定。
3. **完成本人账号与角色。** 在安全界面由本人设置密码、收取确认信并完成恢复验证；维护者
   接续技术操作。确认外部邮件、链接及新密码登录有效后开放生产邮箱入口，核对本人 Auth UUID
   并显式授予站主权限。验证匿名、两个独立访客与站主的边界，以及退出、角色和草稿隔离。
   Supabase/Vercel 服务商账号不能代替网站账号，不在聊天中收取密码、验证码或密钥。
4. **完成真实日常使用。** 用本人同意公开的内容，验证随记／文章、图片、项目的保存、预览、
   发布、修改、撤下与重发，以及访客留言、回复和站主审核。独立会话能读到发布内容，不能看到
   草稿或未审核留言；本人暂不发布的项目明确留待验收，不用公开假作品凑完成。
5. **完成持久化、备份与交付。** 验证内容和媒体跨部署保留，确定长期私密备份位置并执行
   与当前数据范围匹配的备份／隔离恢复验证，保留可回退版本。更新 progress、运行说明、
   邮件／云端／随记验收及相关决策，交付简短发布、审核、恢复说明；分别列明已部署、已实测
   和仍需本人操作的事项。仅当相应真实验收通过，才称平台可以日常使用。

网站新版部署可以先完成，访客注册待邮件验证后开放；域名暂缓不应让所有接续工作停住。
不扩展论坛、私信、复杂权限、架构重写或新视觉变体。修复上线与日常使用中实际阻碍的问题即可。

### 可复制的新任务委托

> 接续本项目的生产上线与真实账号验收。先按 AGENTS.md 阅读启动资料和
> docs/operations/PRODUCTION_HANDOFF.md 的最新接续段，从本地 main 的已验收交付提交开始，
> 保留 Cursor 与本轮成果；当前视觉我已认可，不再探索设计，也不重建已有 site/ + Supabase 后端。
> 核实本地／远程差异并同步代码，部署新版到现有 Vercel；接着完成实际邮件方案与外部送达、
> 本人安全界面设密码／确认／恢复、显式站主授权和独立访客权限，再验收真实内容、媒体、留言审核、
> 重新部署持久化与长期私密备份，并完整更新相关文档。网站继续使用现有地址，域名购买仍暂缓，
> 没有新费用授权；若发件服务需要域名，单独准备具体选择，不要求先决定品牌。
> 常规技术实现、验证、GitHub 同步和部署由你推进；必要的本人登录、收信和密码输入用安全界面完成，
> 不在聊天里索取密码、验证码或密钥。以真实验收完成为终点，不用本机演示身份、样例或旧记录代替。

---

**以下为 2026-09-21／22 原始委托历史。** 保留用于理解最初范围和验收目标，不是当前重做清单。
后端选型、实现、首次部署和视觉探索已完成；与上方最新接续冲突时以上方为准。
历史提交、端口、工作树建议和“前端尚未接入”等状态不得当作当前起点。

## 历史委托：最初的实现范围

用户希望在新的 task 中继续完成剩余工作，由代理承担技术选择、实现、验证、GitHub 同步和
部署执行，尽量只把购买、注册、验证码等必须本人完成的操作留给用户。
不要停在另一份方案、重复的视觉样稿或“可以继续”的答复上。

目标是一个能从零内容开始、方便日常发布的个人创作平台，包括真实的访客账号和留言。
低流量不妨碍上线；AWS 学习、找回过期域名、准备第一批作品都不是前提。
当前前端作为实现起点，视觉仍可按实际体验修改，不代表最终品牌或设计已经获准。
旧个人平台设计 skill 不再约束审美，旧技术栈也不是必须保留的条件。

## 接手位置

按 `AGENTS.md` 的启动顺序阅读，再看以下入口；避免遍历全部历史设计资料。

- [前端说明](../../frontend/README.md)：当前功能、文件边界、命令和验证范围。
- [前端协议](../../frontend/CONTRACT.md)：页面、数据及本机演示的边界。
- [决策 0012](../decisions/0012-independent-frontend-and-rebuild-authority.md)：允许重思旧基础。
- [部署研究](DEPLOYMENT_AND_OPERATIONS_PLAN.md)：候选方案和上线验收，价格及限制需重新核实。
- [设计简报](../design/RESTART_BRIEF.md)：当前反馈；历史提案不等于用户已选择。

交接时的视觉实现基线为 `3cb16b3`。当时 `frontend/` 是 React/Vite + IndexedDB；根目录另有历史
Next/Payload/PostgreSQL 应用。新前端尚未接入这个后端。不能直接部署静态预览并声称完成平台。
已有本机数据按来源保留，不自动迁移或把样例、QA 文章、演示身份放入生产环境。

## 执行顺序

### 1. 完成真实发布

先用当前官方资料和一个小规模集成验证，选择维护成本合理的后端、媒体和部署组合。
比较应服务于落地，只保留一个主实施方案；在实现前记录架构依据和实际约束。
可以复用旧 Next/Payload，也可以替换，依据现有体验、维护成本及运行证据判断。

将当前内容接口接到真正的数据库和媒体存储，完成管理者登录、文章/组图/链接项目、分类、
草稿、预览、发布、更新和撤下。管理界面应适合日常使用，常规发布不需要修改代码。
公开副本与未发布修改隔离；链接稳定；图片和内容不会因应用重启丢失。
同时迁移必要的公开页面和路由，保持中文阅读、看图、返回与空内容体验。

### 2. 完成真实互动

实现访客注册、登录、退出、验证/找回流程，独立留言页、可选的内容讨论、回复、本人留言
管理与站主审核。登录方式由代理结合实际邮件或身份服务选择并验证，不保留假成功的界面。
公开阅读不要求登录，生产页面不暴露本机演示账号或预览控制。

服务端明确区分匿名、访客和管理者。若复用 Payload，先解决当前“任何已登录用户都可信”
的历史访问规则，再开放访客注册；仅隐藏管理入口没有作用。验证跨身份访问、伪造作者、
读取草稿及修改他人留言等情况，并实现与小站规模相称的限流、审核和会话保护。

### 3. 完成部署和交付

选定方案后完成部署配置、迁移、环境变量模板、持久媒体、实际站点地址、可分享的页面链接、
元信息和站点地图。域名可先使用服务商提供的地址；新域名和付费升级不是默认前提。
从用户实际网络验证访问和管理操作，检查重启/重新部署后的数据、备份恢复及版本回退。
提供一份短操作说明：如何发布、审核、导出或恢复，以及服务和费用在哪里管理。

完成开发不等于已经上线。交付时分别说明已实现、已验证、已部署和仍依赖用户操作的部分。
账号或部署条件暂缺时，先完成所有不依赖该条件的代码和本地验证，保留可立即接续的状态。

## 用户只需参与的环节

常规实现、技术方案、兼容性修复、测试和 GitHub 同步由代理推进，不逐项重新询问。
本次委托包含把平台部署起来；不要把历史“本机前端切片”的边界误作本阶段永久禁令。
仍需遵守工具的实际权限要求及不可代理的账号流程。

需要注册、登录授权、验证码、付款或域名购买时，先确定具体服务、用途、套餐、当期价格和
下一步操作，再尽量合并交给用户。不要要求用户学习整套部署技术或自己完成剩余代码。
尚无预算授权，不购买或启用产生新费用的资源；不要在聊天中索取密码、验证码或密钥。
使用安全的登录界面和环境配置渠道，真实秘密不得进入日志、提交或公开截图。
涉及不可逆的数据迁移、删除或新的公共身份，仍需给出具体影响后由用户决定。

## 完成标准

- 空数据库可以正常浏览；从管理界面新增一篇文章、一组照片和一个链接项目，另一个独立
  浏览器会话能看到已发布内容，看不到草稿。修改、撤下与重新发布生效。
- 匿名、两个不同访客和管理者的服务端权限有测试证据；注册、登录、恢复、留言、回复和
  审核在选定运行环境完成真实验证，不以本机昵称模拟代替。
- 内容和图片经应用重启及部署更新仍在；实际演练备份恢复并记录恢复范围。
- 桌面和手机实测零内容、单项和混合内容，核心交互和错误状态可用；正式页面不含测试
  账号、伪造作品、演示控件或开发说明。
- 针对所选架构运行格式、lint、类型、必要单元/集成、生产构建及浏览器检查；记录任何
  未能验证的外部服务环节，不用历史检查结果替代当前证据。
- 可访问的部署地址、简短使用/运维说明、当前决策、可复现配置和已验证代码同步到 GitHub。

不把论坛、私信、关注系统、任意代码上传、通用页面搭建器、复杂专题编排或大量新视觉变体
塞进这次交付。必要的页面修改和发布便利性改进仍在范围内。

## 工作环境交接

新 task 使用独立 Git worktree，清晰提交并同步 GitHub；完成后将已验证成果整合回主线，
避免只留在无人使用的分支中。保留无关改动，禁止强推及覆盖现有内容。
需要并行实现时，按仓库协议分开文件所有权，由一个主代理集成和验收。

旧任务保留了 `http://127.0.0.1:4323/` 供用户查看当前设计。启动前检查进程和端口，
不要因沙箱网络不可达就判断服务已关闭。新开发服务使用不冲突的端口；不同地址的 IndexedDB
互不相通。旧 QA 来源包括 `localhost:4323` 和 `127.0.0.1:4324`，均不应成为生产数据来源。
