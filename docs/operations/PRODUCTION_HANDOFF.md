# 从本机前端到可上线的个人平台

日期：2026-09-22。本文保留本阶段的委托与完成标准；实施已按
[决策 0013](../decisions/0013-production-supabase-and-next.md) 推进。
当前代码、验收和云端状态以 [progress.md](../../progress.md) 为准，实际操作看 [运行说明](RUNBOOK.md)。

## 下一阶段接续

**最新调整（2026-09-22）：本人正在考虑是否保留 Myshkin 451，域名继续暂缓。**
Myshkin 451 只作为工作名，不替本人决定新品牌或重新开启域名购买。自动私有草稿与中文认证
邮件模板已补齐，邮件准备及验证范围见 [认证邮件接续](EMAIL_SETUP.md)。
不再把域名作为本轮
随记上线前置条件；继续使用现有 Vercel 地址。随记按「仅本人发布、公开阅读、草稿私有」
实施，见 [决策 0014](../decisions/0014-single-author-notes.md) 与 [随记验收](NOTES_ACCEPTANCE.md)。
域名、Resend 发件域名验证、真实邮箱/首位站主及本人内容验收仍待接续。下文原排序在这一点
由本次明确调整覆盖，不能据其再次要求本人立刻购买域名。

本 task 可以按“真实后端实现、本机验收、首次云端上线”阶段收尾，完整的线上日常使用验收仍有
以下遗留项。建议新 task 从已同步的 `main` 开始，继续同一项目；下面的原始委托保留作为验收合同，
其中“当时尚无后端”等描述是历史起点，不是当前状态。

已完成：`site/` + Supabase 的真实实现，本机发布、权限、双浏览器和恢复演练，Vercel 正式部署，
公开页面及数据库健康检查，代码合入主线。实际地址是
[myshkin451.vercel.app](https://myshkin451.vercel.app)，证据见 [云端验收](CLOUD_ACCEPTANCE.md)。
邮件注册入口仍关闭，真实站主账号和有内容后的云端完整闭环尚未验收；不能声称整个委托完成。

接下来分三段推进，不把新增需求合成没有结束条件的大任务：

1. **正式域名与日常使用闭环。** 核实域名可注册性、首年/续费价格及注册商要求，给出少量具体选择；
   用户决定域名并完成购买后，配置 DNS、HTTPS、站点正式地址、邮件发件域名和 Auth 回调。
   Brevo 多次手机验证码被拒，停止此前反复换号码和私密 helper 的路径。推荐评估 Resend 接入
   Supabase SMTP，但尚未注册、配置或验证该替代服务。完成真实确认信/恢复信送达、本人站主授权、
   发布/修改/撤下、图片、留言审核和重新部署后的持久化验证，并落实长期私密备份保存方式。
   用本人愿意公开的真实内容做最终体验验收；本人暂不发布时明确保留该项，不用假作品凑通过。
2. **前端与需求迭代。** 以实际使用反馈整理待办，优先处理阻碍发布、阅读、看图和手机操作的问题。
   新增功能按用户目的确定优先级，每轮交付可体验、可验收的一小段；当前视觉不是最终定稿。
   不重新套用旧平台设计 skill，也不把新任务默认扩大成架构重写。
3. **性能与升级评估。** 记录当前访问速度、主要访问地区、图片体积、数据库/存储/流量用量和月度费用。
   区分网络线路、页面资源、数据库查询与套餐额度，再提出优化或升级建议及当期费用。
   当前没有容量不足的实测证据；保持 Vercel/Supabase 免费组合，不能先买套餐再解释需求。
   如果关注中国大陆访问，单独测实际运营商网络；不能把付费升级等同于线路改善。

域名购买、服务升级和新费用尚未获得具体预算授权。常规技术工作继续由代理完成；只把最终域名/
费用选择、付款、不可代理的本人登录验证和必要的产品取舍交给用户。不把检查当前状态变成额外审批。

接手时注意：官方 Vercel/Supabase CLI 已登录，但应确认会话仍有效；已授权 GitHub 仓库范围只有
`myshkin451/myshkin451.com`。优先用官方 CLI/API 执行配置，浏览器用于必要的本人步骤与界面验收；
此前 Chrome 自动控制连接失败，内置浏览器可检查公开网站，不要反复重试同一失效连接。
生产迁移已经应用，不要重复初始化；本机三个验收栈已停止、数据卷保留，临时目录不等于长期备份。

可用于新 task 的委托：

> 接续 Myshkin 451 下一阶段。先按 AGENTS.md 阅读启动资料和本文件“下一阶段接续”，核实当前状态，
> 不重做已完成的后端、部署和随记。域名按我最新决定暂缓；先结合我的随记和前端反馈做迭代，
> 先完成不依赖品牌的体验改进；等我确定名字并恢复域名事项后推进邮件、真实站主与本人发布验收。用实测给出升级建议。常规实现、验证和 GitHub 同步由你执行，
> 域名、购买金额和不可代理的本人验证交给我。不要继续反复尝试 Brevo 手机验证。

## 本次委托

用户希望在新的 task 中继续完成剩余工作，由代理承担技术选择、实现、验证、GitHub 同步和
部署执行，尽量只把购买、注册、验证码等必须本人完成的操作留给用户。
不要停在另一份方案、重复的视觉样稿或“可以继续”的答复上。

目标是一个能从零内容开始、方便日常发布的个人创作平台，包括真实的访客账号和留言。
低流量不妨碍上线；AWS 学习、找回过期域名、准备第一批作品都不是前提。
当前前端作为实现起点，视觉仍可按实际体验修改，不代表最终品牌或设计已经获准。
旧个人平台设计 skill 不再约束审美，旧技术栈也不是必须保留的条件。

## 接手位置

按 `AGENTS.md` 的启动顺序阅读，再看以下入口；避免遍历全部历史设计资料。

- [前端说明](../../frontend/README.md)：当前功能、文件边界、命令和验证范围。
- [前端协议](../../frontend/CONTRACT.md)：页面、数据及本机演示的边界。
- [决策 0012](../decisions/0012-independent-frontend-and-rebuild-authority.md)：允许重思旧基础。
- [部署研究](DEPLOYMENT_AND_OPERATIONS_PLAN.md)：候选方案和上线验收，价格及限制需重新核实。
- [设计简报](../design/RESTART_BRIEF.md)：当前反馈；历史提案不等于用户已选择。

交接时的视觉实现基线为 `3cb16b3`。当时 `frontend/` 是 React/Vite + IndexedDB；根目录另有历史
Next/Payload/PostgreSQL 应用。新前端尚未接入这个后端。不能直接部署静态预览并声称完成平台。
已有本机数据按来源保留，不自动迁移或把样例、QA 文章、演示身份放入生产环境。

## 执行顺序

### 1. 完成真实发布

先用当前官方资料和一个小规模集成验证，选择维护成本合理的后端、媒体和部署组合。
比较应服务于落地，只保留一个主实施方案；在实现前记录架构依据和实际约束。
可以复用旧 Next/Payload，也可以替换，依据现有体验、维护成本及运行证据判断。

将当前内容接口接到真正的数据库和媒体存储，完成管理者登录、文章/组图/链接项目、分类、
草稿、预览、发布、更新和撤下。管理界面应适合日常使用，常规发布不需要修改代码。
公开副本与未发布修改隔离；链接稳定；图片和内容不会因应用重启丢失。
同时迁移必要的公开页面和路由，保持中文阅读、看图、返回与空内容体验。

### 2. 完成真实互动

实现访客注册、登录、退出、验证/找回流程，独立留言页、可选的内容讨论、回复、本人留言
管理与站主审核。登录方式由代理结合实际邮件或身份服务选择并验证，不保留假成功的界面。
公开阅读不要求登录，生产页面不暴露本机演示账号或预览控制。

服务端明确区分匿名、访客和管理者。若复用 Payload，先解决当前“任何已登录用户都可信”
的历史访问规则，再开放访客注册；仅隐藏管理入口没有作用。验证跨身份访问、伪造作者、
读取草稿及修改他人留言等情况，并实现与小站规模相称的限流、审核和会话保护。

### 3. 完成部署和交付

选定方案后完成部署配置、迁移、环境变量模板、持久媒体、实际站点地址、可分享的页面链接、
元信息和站点地图。域名可先使用服务商提供的地址；新域名和付费升级不是默认前提。
从用户实际网络验证访问和管理操作，检查重启/重新部署后的数据、备份恢复及版本回退。
提供一份短操作说明：如何发布、审核、导出或恢复，以及服务和费用在哪里管理。

完成开发不等于已经上线。交付时分别说明已实现、已验证、已部署和仍依赖用户操作的部分。
账号或部署条件暂缺时，先完成所有不依赖该条件的代码和本地验证，保留可立即接续的状态。

## 用户只需参与的环节

常规实现、技术方案、兼容性修复、测试和 GitHub 同步由代理推进，不逐项重新询问。
本次委托包含把平台部署起来；不要把历史“本机前端切片”的边界误作本阶段永久禁令。
仍需遵守工具的实际权限要求及不可代理的账号流程。

需要注册、登录授权、验证码、付款或域名购买时，先确定具体服务、用途、套餐、当期价格和
下一步操作，再尽量合并交给用户。不要要求用户学习整套部署技术或自己完成剩余代码。
尚无预算授权，不购买或启用产生新费用的资源；不要在聊天中索取密码、验证码或密钥。
使用安全的登录界面和环境配置渠道，真实秘密不得进入日志、提交或公开截图。
涉及不可逆的数据迁移、删除或新的公共身份，仍需给出具体影响后由用户决定。

## 完成标准

- 空数据库可以正常浏览；从管理界面新增一篇文章、一组照片和一个链接项目，另一个独立
  浏览器会话能看到已发布内容，看不到草稿。修改、撤下与重新发布生效。
- 匿名、两个不同访客和管理者的服务端权限有测试证据；注册、登录、恢复、留言、回复和
  审核在选定运行环境完成真实验证，不以本机昵称模拟代替。
- 内容和图片经应用重启及部署更新仍在；实际演练备份恢复并记录恢复范围。
- 桌面和手机实测零内容、单项和混合内容，核心交互和错误状态可用；正式页面不含测试
  账号、伪造作品、演示控件或开发说明。
- 针对所选架构运行格式、lint、类型、必要单元/集成、生产构建及浏览器检查；记录任何
  未能验证的外部服务环节，不用历史检查结果替代当前证据。
- 可访问的部署地址、简短使用/运维说明、当前决策、可复现配置和已验证代码同步到 GitHub。

不把论坛、私信、关注系统、任意代码上传、通用页面搭建器、复杂专题编排或大量新视觉变体
塞进这次交付。必要的页面修改和发布便利性改进仍在范围内。

## 工作环境交接

新 task 使用独立 Git worktree，清晰提交并同步 GitHub；完成后将已验证成果整合回主线，
避免只留在无人使用的分支中。保留无关改动，禁止强推及覆盖现有内容。
需要并行实现时，按仓库协议分开文件所有权，由一个主代理集成和验收。

旧任务保留了 `http://127.0.0.1:4323/` 供用户查看当前设计。启动前检查进程和端口，
不要因沙箱网络不可达就判断服务已关闭。新开发服务使用不冲突的端口；不同地址的 IndexedDB
互不相通。旧 QA 来源包括 `localhost:4323` 和 `127.0.0.1:4324`，均不应成为生产数据来源。
