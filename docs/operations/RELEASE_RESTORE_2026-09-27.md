# 2026-09-27 备份恢复演练

本记录分开保留两类证据：全新合成数据覆盖账号、内容、媒体与权限恢复；实际生产归档覆盖
当时的空站点数据及其隔离恢复。两者不能相互替代，也不代表真实用户发布或邮件验收完成。

## 一、合成数据恢复

本次使用两个全新本机 Supabase 项目验证当前备份恢复实现。所有账号、密码、内容和媒体均为
本次脚本生成的测试夹具；邮箱使用 `example.test`。未读取既有本机项目的数据或凭据，未访问云端，
未复制真实用户数据。此结果不代表生产备份、长期保存位置或生产账号验收已经完成。

### 环境与范围

- 合成源：`myshkin451-release-fixture`，API `127.0.0.1:55821`，数据库端口 `55822`。
- 隔离目标：`myshkin451-release-restore`，API `127.0.0.1:55721`，数据库端口 `55722`。
- 启动前检查两组所需端口可用。两个项目均从仓库配置、迁移及邮件模板建立，关闭 seed，
  不复制其他项目的配置秘密或持久卷。
- 两个项目在写入夹具或恢复前均检查：11 张范围表为空，只有迁移的默认站点设置一行；媒体桶为空。
- 迁移：`202609210001_platform.sql`、`202609210002_notes.sql`、`202609270001_accounts.sql`。
- PostgreSQL、`pg_dump`、`psql` 均为 17.6；Node 为 24.15.0。
- 清单记录的 Git HEAD 为 `766781ab8c928578813d622dce732272dd1449b2`，`dirty=true`。
  演练使用当时工作树的脚本与上述迁移，不能将该 HEAD 单独视为全部受测改动的不可变版本。

测试脚本及仅含合成数据的备份位于仓库外临时目录
`/private/tmp/myshkin-synthetic-restore-pt96khdl/`。备份目录权限为脚本要求的 `700`，文件为 `600`。
CLI 状态中的本机密钥仅在进程内存中传递，未打印，未写入凭据文件。临时目录可能被系统清除，
不作为长期备份目的地。

### 实际结果

脚本正常退出，**107 项检查通过**。归档完整性检查、实际恢复及重启后验证均执行成功。

| 项目 | 结果 |
| --- | --- |
| 恢复范围 | 11 张表，共 14 行 |
| 账号 | 2 个随机生成且已确认的邮箱密码账号，分别为站主和受限访客 |
| 内容 | 1 个已发布条目及其独立私有修订草稿 |
| 媒体 | 1 个生成的单像素 PNG，恢复与重启后原始字节 SHA-256 均一致 |
| 留言 | 1 条访客留言，已由站主审核通过；对应速率记录保留 |
| 权限状态 | 1 条账号限制记录及 1 条权限变更审计记录完整保留 |
| 重启 | 隔离目标全部 7 个运行容器重启后再次验证成功 |

恢复前后逐表行数一致，除 `auth.users` 外的全部表内容摘要一致。`auth.users` 按恢复规则排除
旧一次性邮件令牌字段后逐行一致，包括账号 UUID 和密码哈希。未将旧邮件令牌或登录会话作为恢复承诺。
恢复脚本在上传前核对私有媒体桶大小及 MIME 限制，并逐个上传、下载核对媒体字节。

恢复后和目标重启后都通过以下检查：

- 两个合成账号使用生成时的原密码登录，UUID 保持不变。
- 站主保有管理权限，可以查看账号列表及执行保持自身权限不变的管理请求；撤销自身站主权限被拒绝。
- 受限访客可以登录、查看自己的受限状态，但留言写入与账号管理请求均以权限错误拒绝。
- 受限访客查询草稿得到空结果，匿名请求草稿被拒绝。
- 业务表、账号限制和审计内容在重启及权限验证后保持一致。
- 合成源在生成备份后未继续写入，其 11 张表在全部恢复与重启检查结束后仍与备份前摘要一致。

### 收尾与验收边界

演练结束后两个新建测试项目均已停止，保留其 Docker 持久卷。既有本机项目和生产服务未改动。
若后续清理合成卷，应明确选择上述两个项目，不能将其他项目包含在清理范围内。

本次证明当前迁移及脚本可以恢复本机邮箱密码账号、业务数据、媒体、限制与审计，并在容器重启后保留。
生产归档隔离恢复的独立证据见第二部分。外部邮件、真实账号和内容、长期独立私密副本及周期执行
仍需要各自的实际证据。
此前拟复制既有本机数据的演练未获自动审批，未执行；本次通过改用全新随机合成数据消除了该复制范围。

## 二、实际生产归档隔离恢复

### 来源与隔离边界

源归档是用户授权保存在本机长期私密目录的
`~/Library/Application Support/Myshkin451/Backups/2026-09-27-production`，
创建时间为 `2026-09-26T18:44:49.526Z`，即上海时间 2026-09-27 02:44。
它包含 11 张表范围内的 1 行默认站点设置，账号、内容和媒体均为 0。

恢复过程只读取该现有归档，没有再次访问云端或读取云端凭据。新目标为
`myshkin451-production-archive-restore`，API `127.0.0.1:55921`，数据库端口 `55922`。
配置、三份迁移及邮件模板从当前仓库建立；seed 关闭。启动前检查所需端口空闲，恢复前验证
11 张表均为空，唯一例外是迁移写入的默认站点设置。既有本机环境和第一部分的合成项目未改动。

脚本及仅含计数、状态的证据在仓库外
`/private/tmp/myshkin-production-restore-2mz7un6k/`，该临时目录不是生产备份保存位置。

### 实际结果

**54 项检查通过**，脚本正常退出：

- 先执行 `restore.mjs --verify-only` 验证归档，再实际恢复至新的空目标。
- 恢复前的列结构与归档一致；恢复后的 11 张表行数及内容摘要逐项匹配归档。
- 匿名公共查询可读取 1 行默认站点设置，值为 `Myshkin 451`、空介绍、空关于及网格视图。
  已发布内容和公开留言均为空；匿名草稿请求被拒绝。
- 重启目标的全部 7 个运行容器，等待 Auth 健康检查通过，再次核对全部表行数、摘要和公共查询。
- 最终仍为 11 张表、1 行默认设置、0 账号、0 媒体。原归档未修改。

新目标已停止并保留 Docker 持久卷，没有清理、重置或停止其他项目。

### 证明范围

这次实际演练证明当时的生产归档能恢复到匹配迁移的本机隔离 Supabase，并在重启后保留。
**空归档不能证明真实账号、作品或媒体的恢复**；相关功能由第一部分的 107 项合成检查覆盖。
真实数据产生后，应再对相应生产归档演练，不把这次空站点结果当作永久覆盖所有版本与数据的保证。
