# Claude Code Entry

Claude agents must follow the shared repository instructions in `AGENTS.md`.

Do not maintain a separate rule set in this file. If the agent workflow changes, update `AGENTS.md` and keep this file as a pointer.

Startup path:

1. Read `AGENTS.md`.
2. Follow the startup path defined there.
3. Use `progress.md` as the current state board.
4. Use `docs/HARNESS.md` for validation, handoff, and multi-agent workflow.

